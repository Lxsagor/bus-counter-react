import { makeStyles } from "@mui/styles";
export const useStyles = makeStyles((theme) => ({
    button: {
        "& .MuiButton-root": {
            borderRadius: "25.8199px",
            backgroundColor: "#000000",
            minWidth: "147px",
            minHeight: "55px",

            textTransform: "initial",
        },
    },
}));
